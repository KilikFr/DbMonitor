{% extends "KilikTableBundle::_defaultTable.html.twig" %}

{# add a column to the left #}
{% block tableHeadStdColumns %}
    <th>
        &nbsp;
    </th>
    {{ parent() }}
{% endblock tableHeadStdColumns %}

{% block tableHeadStdFilters %}
    <td>
        &nbsp;
    </td>
    {{ parent() }}
{% endblock tableHeadStdFilters %}

{% block tableBodyStdColumns %}
    <td>
        <a href="{{ path('kilik_db_monitor_evolution_history',{'history':'hourly', 'server': row.object.id, 'date': lastTime|date('Y-m-d_H:i') }) }}">H</a>
        <a href="{{ path('kilik_db_monitor_evolution_history',{'history':'daily', 'server': row.object.id, 'date': lastTime|date('Y-m-d_00:00') }) }}">D</a>
        <a href="{{ path('kilik_db_monitor_evolution_history',{'history':'monthly', 'server': row.object.id, 'date': lastTime|date('Y-m-01_00:00') }) }}">M</a>
    </td>
    {{ parent() }}
{% endblock tableBodyStdColumns %}
